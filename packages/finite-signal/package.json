{
  "name": "finite-signal",
  "version": "0.0.1",
  "description": "A user-friendly finite state machine library for TypeScript",
  "devDependencies": {
    "@swc/core": "^1.3.14",
    "@swc/helpers": "^0.4.12",
    "@swc/jest": "^0.2.23",
    "@types/jest": "^29.2.2",
    "@types/node": "^17.0.12",
    "jest": "^29.3.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "dev": "ts-node-dev --transpile-only ./src/example.ts",
    "test": "jest --maxConcurrency=30",
    "test:watch": "jest --watch --maxConcurrency=30",
    "build": "swc ./src -d ./dist --source-maps --minify"
  }
}
